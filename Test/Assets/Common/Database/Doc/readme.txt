DB設計書はDB(SQLiteのファイル単位)
※configDBはmasterに統合するかも

はDB作成に使います。
DBファイルの自動生成を使う場合、同じディレクトリにおいてください。
sqlite3.dll
SQLite3StdCall.dll

src/Dao,src/DaoBase,src/Dto,src/DtoBase,db
フォルダは自動生成の出力先なので、同じ階層にディレクトリを作っておいてください。